import type { LearningSummary } from '../types';

export function LearningDashboard({ summary, onDelete }: { summary: LearningSummary | null; onDelete: (threadId: string) => Promise<void> }) {
  if (!summary) return <div className="rounded-3xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-600">Loading your practice evidence…</div>;
  return <section className="space-y-5"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-teal-700">My Learning</p><h1 className="mt-1 text-3xl font-black">Practice evidence, explained</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">{summary.progress_meaning}</p></div><div className="rounded-3xl bg-slate-950 p-6 text-white"><p className="text-xs font-bold uppercase tracking-wider text-teal-300">Recommended next skill</p><h2 className="mt-2 text-xl font-black">{summary.recommendation.skill.replaceAll('_', ' ')}</h2>{summary.recommendation.title && <p className="mt-2 font-semibold text-teal-200">Try next: {summary.recommendation.title}</p>}<p className="mt-2 text-sm text-slate-300">{summary.recommendation.reason}</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{summary.skill_evidence.map(item => <article key={item.criterion_id} className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-sm font-bold">{item.criterion_id.replaceAll('_', ' ')}</p><p className="mt-3 text-3xl font-black text-teal-700">{item.count}</p><p className="text-xs text-slate-500">demonstrated practice examples</p></article>)}</div>{summary.sessions.length > 0 && <div className="rounded-3xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-black">Session history</h2><div className="mt-4 space-y-3">{summary.sessions.map(session => <article key={session.thread_id} className="flex items-start justify-between gap-4 rounded-xl bg-slate-50 p-4"><div><p className="text-sm font-bold">{session.situation || 'Context session'}</p><p className="mt-1 text-xs text-slate-500">{session.status}</p></div><button type="button" onClick={() => onDelete(session.thread_id)} className="text-xs font-bold text-rose-700">Delete session</button></article>)}</div></div>}</section>;
}
